import React from "react";

import NotFound from "@/components/ui/NotFound";

const NotFoundPage = () => {
  return (
    <section>
      <NotFound />
    </section>
  );
};

export default NotFoundPage;
